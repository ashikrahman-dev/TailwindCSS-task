/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["*"], // * for all files.
    theme: {
        extend: {},
        container: {
            center: true,
            padding: "2rem",
        },
    },
    plugins: [],
};
